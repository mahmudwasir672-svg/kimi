/**
 * SheetMail Sender - Entry Point
 * ==============================
 * Main entry point for the application
 */

import { registerRootComponent } from 'expo';
import App from './App';

// Register the app
registerRootComponent(App);
